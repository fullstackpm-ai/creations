# MCP configuration (contains credentials)
.mcp.json

# Environment files (credentials)
.env
**/.env

# OS files
.DS_Store

# Raw meeting transcripts (privacy)
docs/meetings/.raw/*
!docs/meetings/.raw/.gitkeep
