{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -q 'gh pr create' && echo '✓ PR created - PR-Agent will auto-review when pushed to GitHub' || true"
          }
        ]
      }
    ]
  }
}
